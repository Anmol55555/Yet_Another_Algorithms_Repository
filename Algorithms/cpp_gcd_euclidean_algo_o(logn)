#include <iostream>

using namespace std;

int gcd(int a, int b)
{
    if(b == 0)
        return a;
    else
        return gcd(b, a%b);
}

int main()
{
    int a,b;
    cout<<"Enter the two values for finding GCD"<<endl;
    cin>>a>>b;
    cout<<"The GCD of the given number is "<<gcd(a,b);
}
